#spring:
#  redis:
#    host: 192.168.23.131
#    port: 6379
#    lettuce:
#      pool:
#        max-active: 8  # 最大连接
#        max-idle: 8  # 最大空闲连接
#        min-idle: 0  # 最小空闲连接
#        max-wait: 100  # 连接等待时间

server:
  port: 8081
spring:
  application:
    name: hmdp
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/secondsql?useSSL=false&serverTimezone=UTC
    username: root
    password: 1211
  redis:
    host: 192.168.23.131
    port: 6379
#    password: 123321
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 1
        time-between-eviction-runs: 10s
  jackson:
    default-property-inclusion: non_null # JSON处理时忽略非空字段
mybatis-plus:
  type-aliases-package: com.hmdp.entity # 别名扫描包
logging:
  level:
    com.hmdp: debug